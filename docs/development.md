# Stash Master 开发说明

> Parent: [index.md](index.md)
> 用户说明：[English](../README.md) / [简体中文](../README.zh-CN.md)

本文承接从 README 迁出的开发进度、实现说明、诊断与验证记录。
完整行为契约由 `feats/` 维护，各项实现与验收进度由 `plans/` 记录。

## 项目标识与部署

- 项目名称：Stash Master。
- 仓库：<https://github.com/ChouUn/spt-stash-master>。
- 解决方案：[StashMaster.sln](../StashMaster.sln)。
- 根命名空间：`ChouUn.StashMaster`，Core 和测试工程使用对应子命名空间。
- 程序集：`ChouUn.StashMaster.dll`、`ChouUn.StashMaster.Core.dll`。
- BepInEx 插件标识：`com.chouun.stashmaster`。

[部署脚本](../scripts/deploy.ps1)将构建产物复制到
`BepInEx/plugins/ChouUn.StashMaster/`。游戏运行时拒绝部署。

## 开发进度与验收记录

- MVP 九个步骤均已完成并通过对应验收：tag 编辑、折叠、合并堆叠、按规则收纳、
  启发式与 CP-SAT 排布，以及 UIFixes 6.0.2 排序入口兼容。
  记录见 [MVP 实施计划](plans/mvp.md)。
- 第 8 步验收确认：收纳后合并正常；容器已满但堆叠未满时也能补充数量。
  第 9 步排布与空间选择已通过游戏内验收，均记录于 MVP 计划。
- 后续多网格联合选择、跨容器统一堆叠、2+1 求解预算与减少重复读取已实现，
  自动化验证和待验收项见 [收纳优化计划](plans/collection-optimization.md)。
- 父类优先的层级聚合效果已获用户确认，见 [类别聚合计划](plans/category-grouping.md)。
  后续全部容器联合规划、按网格计分和停滞退出的实现与验证，
  见 [全局排布计划](plans/global-packing.md)。
- 全局几何搜索前先按类别与行分配等尺寸占位；排序与聚合共用 SortType 顶层树，
  数量与几何模型复用节点范围，几何对称性不按模板身份拆分等价组。
- 分类边界已修复：仅保留 SortType 对应原生类别之下的子链，跨类物品直连业务类型，
  GP 币不再把“贵重物品”带到 Money 下；本地化 tag 类别链不变。
  旧离线回放遗漏 mod 注册数据，旧构树又错误拼接原生祖先；此前聚合评分不再作为真实游戏验收依据。
  2026-09-12T23:46:48Z 从运行中服务端重新采集手册、模板和本地化数据，
  核对原始五网格的 503 件物品（498 可移动、5 固定）、278 个模板：全部模板存在，
  277 个有完整手册链，USEC 狗牌确实未登记手册；旧输入漏用 15 个模板、16 件物品的注册。
  修复后四件货币均直属 Money、八件饮料均直属 Drink，枪械、配件、医疗内部细分保留。
  六次共享 3 秒预算的冷回放及缓存输入/输出重放通过守恒、边界、固定障碍、类型顺序和同口径总分不退步检查；
  五网格可移动物品占用高度均保持 `[61,8,7,10,4]`。Core 199、Plugin 47 项回归通过，生产插件 Debug/Release 构建通过。
  数据来源、全量覆盖、树与回放证据保留在 `.temp/classification-repair/` 的 JSON 文件中。
  本轮使用原始快照几何与重新核验的运行时分类，不是新采集的游戏内布局；未部署，未做游戏内画面验收。
- 后续补齐 `Flyer → Barter` 映射：海报沿运行时手册归入“杂物 / 其他”，不再成为未识别类型。
  新回归覆盖真实 Flyer 类型与内部子链，Plugin 48 项测试及生产插件 Release 构建通过。
  上述五网格 JSON 回放记录对应补齐 Flyer 前的版本；本次未重跑全仓布局、未部署。
- 修复单一类型容器被排除在压紧提示之外的问题：去掉“必须存在类型先后关系”的门槛，
  显式保留全部物品必留、完整布局、顺序合法和存在空间余量的限制，求解预算不变。
  匿名多网格回归修复前返回 8 行、修复后得到 7 行；Core 200 项回归通过。
  最新五网格输入完成 20 次清缓存、每次 3 秒预算的离线回放，17 个弹匣全部压至 7 行，
  五网格高度均为 `[61,7,7,10,4]`，耗时约 2.16–2.56 秒；守恒、几何、类型顺序与缓存检查通过。
  证据见 `.temp/performance-acceptance/compaction-final/`；本次未启动游戏、未部署。
- 补充重复排序稳定性需求：尽可能一次收敛，无质量收益时保留逐物品布局与身份；
  后续仍有真实改善也记为首次收敛不足，不能仅用高度不变或缓存命中判定稳定。
  `a5ef3c4` 在既有五网格样本上完成三种场景、各 5 组 × 8 次连续离线排序，共 120 次；
  每次输出作为下次输入，并按位置重建物品枚举。分类与输入来源经 49 项文件指纹核对未变。
  同缓存、固定 3 秒：5 组均从第二次起无移动，后续 35 次均命中缓存。
  每次重建缓存、固定 3 秒：4/5 组第二次仍移动 24–59 件，第三至第八次未再变化。
  同缓存、预算从 2.950 逐次增至 3.000 秒：3/5 组仍有后续移动，其中两组第七次又移动 28、44 件；
  连续几次不动不能作为永久收敛的证明。预算序列为受控实验，不是逐次复制游戏日志。
  后续移动均发生在根仓库且带来类别评分改善，无新增旋转、无等分换位；五网格高度始终为
  `[61,7,7,10,4]`。全部调用通过物品守恒、几何、固定障碍、类型顺序与评分不退步检查。
  稳定性需求尚未满足；证据与回放程序在 `.temp/sort-stability/`。仅验证最终排布，
  未覆盖折叠、堆叠、收纳、游戏事务或真实重启；不是当前日志的十一网格状态。未改算法、未部署。
- 将类别占位提示与联合几何的剩余预算由 1:1 调整为 2:1；准备和压紧耗时先扣除，
  整次 3 秒上限不变，双方仍采用 0.5 秒无改善停滞阈值，占位未用完额度交给几何。
  既有五网格样本完成旧、新比例各 5 组 × 4 次连续回放，共 40 次，绕过缓存并逐次应用输出。
  新比例首次同口径质量在 4/5 组更好；第二次移动数由旧比例的 41–92 件降到 15–45 件，
  但两种比例的 5/5 组均有后续移动，新比例仍有一组第三次不动、第四次又改善，尚未满足一次收敛。
  首次耗时中位数由 2.673 秒增至 3.018 秒；新比例全部调用范围 2.131–3.031 秒。
  五网格高度均为 `[61,7,7,10,4]`，守恒、几何、固定障碍、类型顺序及评分不退步检查通过。
  Core 测试 199 通过、1 失败：护甲优先真实仓库预期 61 行、实际 62 行；未修改或放宽断言。
  对该测试的两个精确输入另做 20 次旧、新比例对照：SMH 默认均 5/5 次为 61 行，
  护甲优先均 5/5 次为 62 行，失败并非新比例独有。Plugin 48 项通过，Release 插件构建通过。
  证据见 `.temp/packing-budget-2to1/`；仅离线最终排布验证，未部署，不能据此宣称稳定性已修复。
- 【已回撤的实验】最终排布曾改为按可移动物品数从少到多串行处理独立网格；同数量保持输入顺序，
  结果仍按原网格对应。每格领取当前余量除以待处理格数的额度，先压紧、再按 2:1 做占位与几何；
  每阶段保持 0.5 秒停滞阈值，已到最优界限的网格跳过，未用时间交给后续网格，收纳联合选择不变。
  旧联合、新独立各 5 组 × 4 次回放通过守恒、几何、固定障碍、类型顺序及评分不退步检查，
  新版全部调用验证了先小后大的顺序；首次依次处理 17 件武器箱、99 件垃圾箱、301 件根仓库。
  五网格高度均为 `[61,7,7,10,4]`。独立版首次质量 3/5 组更好，但总分中位数略差；
  首次耗时中位数由 2.921 秒变为 3.012 秒，后续由 1.859 秒变为 2.371 秒。
  旧版 3/5 组、新版 4/5 组第二次仍有改善，第三、四次未变；不能据此宣称拆分更快或已解决收敛。
  Core 200、Plugin 48 项测试通过，Release 插件构建零警告、零错误；既有护甲优先测试本次通过，
  不抹去前述限时失败记录。证据见 `.temp/independent-grid-packing/`；未部署、未做游戏内验收。
- 【已回撤的实验】随后修正“从小到大”的尺度：按容器全部网格面积之和排序（包含空网格和固定占位），
  整理根容器固定最后，不再按可移动物品数量；等面积保持输入顺序，返回结果仍对应原网格。
  合成场景验证了 50 格容器 → 1050 格多网格容器（含 1000 格空网格）→ 60 格根容器。
  同一五网格样本扩大为旧联合、新空间顺序独立各 100 组 × 连续 4 次，共 800 次，
  每组从相同起点开始，后续承接上次布局并按行重新枚举；交替版本顺序、串行执行、不使用结果缓存。
  新版首次总分 25 胜、20 平、55 负（越低越好）；全部首次高度仍为 `[61,7,7,10,4]`。
  首次耗时中位数/P90/P95：旧版 2.874/2.912/2.933 秒，新版 3.010/3.019/3.020 秒；
  后三次为旧版 1.812/1.981/2.118 秒，新版 2.309/2.557/2.648 秒。
  旧版 37/100 组、新版 59/100 组后续仍改变；最后观察到改变在第 1/2/3/4 次的组数，
  分别为旧版 63/25/10/2、新版 41/48/10/1。此处不是永久收敛次数，存在一次不动后再次改善。
  后续位置或朝向变更累计旧版 2073、新版 3106，均在根仓库；无等分搬动，无总分退步。
  800 次全部通过守恒、几何、固定障碍、类型顺序检查；新版每次核验空间顺序。
  Core 200、Plugin 48 项测试通过，Release 构建零警告、零错误。
  此样本中活跃网格的面积顺序恰好与旧物品数顺序相同，尺度修正由上述合成场景区分验证。
  证据见 `.temp/container-area-100/`。只扩大了固定样本重复次数，未部署、未做游戏内验收；
  本样本结果不支持独立拆分更快、首次质量更好或收敛已修复，也不代表其他容器组合的结论。
- 2026-09-14 按上述 800 次对照结果回撤独立排布：主要耗时、首次质量与重复排序稳定性均劣于旧联合版，
  不将该方案保留为优化。样本中的面积顺序与物品数顺序相同，不能把退步归因于面积尺度修正。
  恢复实验对照中的旧联合入口：先准备各网格保底与压紧提示，再联合求解全部仍可改善的网格。
  删除仅供独立调度使用的 `PackRequest.ContainerArea`、`IsRootContainer` 及规划器、缓存中的对应处理；
  不保留独立模式或兼容开关。测试撤销“每格分别不退步”的独立调度断言，保留完整总分不退步与归属检查。
  保留此前的占位/联合几何 2:1、0.5 秒停滞阈值、整次 3 秒预算、分类树修正及原位身份回填；收纳流程不变。
  回撤后同一五网格样本连续 4 次无缓存回放通过守恒、几何、固定障碍、类型顺序与总分不退步检查，
  首次变更 227 件（其中转置 7 件），后续三次均未变；这是回撤冒烟验证，不是收敛已修复的证明。
  Core 200、Plugin 48 项测试全部通过，Release 插件构建零警告、零错误。
  回撤证据见 `.temp/joint-packing-rollback/`；原对照数据与源码归档保留在 `.temp/container-area-100/`。
  同步恢复需求、架构、排布契约与全局排布计划。未部署、未修改游戏存档、未做游戏内验收。
- 按新垃圾箱快照恢复末端 `TemplateId` 虚拟类别，直接沿用原树的裁剪、压缩和层级权重，
  不另设跨分支的模板优先级；占位数量分组、几何对称性与实际评分统一使用模板叶节点。
  模板主题首轮独立验证曾通过 Core 207 项；审查修订后的隔离全量对照为 HEAD 199/200、暂存版 206/207，均只剩同一基线限时失败；Release 构建通过，详见[模板聚合报告](reports/template-grouping.md)。
  下述存档回放同时包含模板和方向目标，不能作为仅模板主题的独立性能证明。
  每网格完整分数在类别层之后追加占用格子的行坐标和、列坐标和，依次偏好向上、向左；
  转置按实际矩形占格计分，未选候选贡献为零，固定障碍不计入目标；同分保留当前布局。
  两种模型、实际比较、类别顺序评分上界及提前结束条件同步更新；联合求解、2:1 预算和 0.5 秒停滞不变。
  固定存档 `.temp/stash-snapshots/20260914-204514-487910Z-template-grouping-upper-left/` 中目标垃圾箱
  `6aa5e5854fac5f8098ac36da` 含 69 件、35 种模板；静态库、缺失的模组软盘定义与已部署插件一并归档。
  用当前生产分类代码核对全部模板，并按单独捕获的实际类别顺序配置回放同一垃圾箱，连续三次均不使用缓存。
  三把火镰从 `(8,0)/(9,0)/(8,1)` 聚到 `(7,0)/(8,0)/(9,0)`；占用高度保持 7 行，
  树跨度向量从 `[6,11,5]` 变为 `[6,11,3]`；占用格子行坐标和 294→293，列坐标和 591→599。
  列目标是最低优先级，不要求每个分量同时改善。右上空腔未消除：第一行仍空 3 格、第二行空 1 格。
  三次耗时约 1.096/0.787/0.728 秒（首次含 JIT/原生启动），移动数 26/0/0；均为停滞退出的可行解，未证明最优。
  逐件守恒、边界、固定障碍、类型顺序、实际占格坐标和与计分一致性、完整总分不退步检查全部通过；
  前后 SVG 已检查。证据见 `.temp/template-upper-left-replay/verified/`，仅覆盖该垃圾箱，不代表全仓库共享预算表现。
  当时模板与方向联合实现的历史验证为 Core 209、Plugin 48 项通过、Release 构建零警告零错误；不代替本次方向主题暂存验证。
  穷举正确性测试加入坐标目标并显式使用 5 秒预算，不把默认一秒限时当作最优证明；
  删除旧样本“最多移动 94 件”的偶然阈值，收纳测试提供真实当前布局，堆叠测试修正两个容器重叠的非法输入。
  当时既有护甲优先 61 行限时测试在中间回归曾得到 62 行，最终回归通过；这是历史结果，未修改或放宽其断言。
  原始完整存档保持不变；本次未提交、未部署，未做游戏内事务验收，未宣称一次收敛或上方空洞问题已完全解决。
- Debug 原生对比修复后的游戏日志已核验，五个网格均输出对照且未应用原生结果，
  见 [排序对照报告](reports/sort-comparison.md)。

这些记录对应各自版本与验收范围；后续改动的自动化验证不替代其游戏内验收。

## 实现与规则入口

阶段顺序为折叠、统一堆叠、按规则收纳、最终排布，
模块边界和游戏事务流程见 [总体设计](arch.md)。

- [tag 语法](feats/tag-grammar.md)：规则解析、匹配与优先级。
- [堆叠契约](feats/stacking.md)：容器内优先补充 Pinned 堆叠，
  其余把小堆并入较大的堆叠；统一规划跨容器来源，按实际事务结果更新数量。
  合并能力与上限由游戏校验，通知中的“合并 N 次”按成功事务计数。
- [排布契约](feats/packing.md)：收纳只最大化装入面积，
  最终排布在每个网格内计算高度、父子类别及模板跨度，追加占用格子的行列坐标和，再累加完整分数。
  CP-SAT 只在对应阶段目标改善时替换合法基线。

搜索共享整次 3 秒预算：收纳最多使用 2 秒，单个目标容器的联合选择最多分配 1 秒；
最终排布先准备保底与压紧提示，再联合求解仍可改善的全部网格，共享剩余额度。
占位提示与联合几何按 2:1 分配准备和压紧后的余量，各自连续 0.5 秒无改善时提前停止；
提示比较本网格完整分数，联合几何比较所有参与网格完整分数之和，详细边界见排布契约。
未变化的输入与已应用布局可命中缓存，复用结果不代表已证明最优。
后台搜索以已有可行布局保底；游戏事务、调度与首次原生库初始化另有开销，
3 秒不是整次整理的耗时上限。

## 排序入口兼容

### UI Fixes

UIFixes 6.0.2 的入口兼容于 2026-09-10 通过游戏内验收。
插件通过软依赖等待 UIFixes 注册补丁，精确移除
`GridSortPanel.Sort` 上的 `UIFixes.SortPatches+StackFirstPatch.Prefix`。
代码见 [UIFixesCompatibility.cs](../src/Plugin/Compatibility/UIFixesCompatibility.cs)。

正式整理不调用 UIFixes 的 Sort 或原生排序计算，堆叠由本项目通过游戏 API 执行。
UIFixes 的其他补丁、配置文件与 FiR 混堆规则保留。
移除本项目并重启游戏后，UIFixes 按自己的配置重新注册入口。
已安装版本的目标方法或补丁注册不符时，插件记录兼容失败并停止接管排序。

### Stash Management Helper

尚未实现兼容适配，也未完成游戏内验收。
普通按钮在 `GridSortPanel.Sort` 被接管，不进入 SMH 的
`ItemManipulator.Sort` 折叠、堆叠与排序流程。
SMH 右键菜单的 `SwapFlags` 会备份并临时改写排序选项，然后调用面板排序；
恢复位于 SMH 的排序后置补丁，接管后可能不会运行。
因此当前不能声明完整兼容，用户可见影响保留在两种语言的 README 中。

## Debug 原生排序对比

Debug 构建在最终规划后，使用相同物品、网格与固定障碍，
在独立占用缓冲上测量原生列表排序与落位。结果不应用，不提交游戏事务。
诊断代码位于 [Plugin/Diagnostics](../src/Plugin/Diagnostics/)，
行为边界见 [排布契约](feats/packing.md)。

- 日志以 `sort-benchmark` 开头，记录是否全部放入、行数与类别跨度。
- 基准额外耗时单独记录，从正式整理耗时中扣除，不占求解预算。
- 测量不包含原生控制器校验、事务回滚与 UI 刷新。
- 复制完整原生空位搜索调用链，避免调用 UIFixes 已修改的方法；
  正常游戏调用仍保留第三方补丁与配置。
- Release 编译时排除基准入口和实现，不执行原生排序对比。

## 性能与复现资料

- [原生与 SMH 对照](reports/sort-comparison.md)：
  同一散乱快照上的离线计算、布局质量和后续游戏日志，包含版本、配置与测量范围。
- [武器箱排序边界](reports/weapon-sort-boundaries.md)：
  原生与 SMH 各自成功或失败的构造，完整原生排序方法、回滚与几何守恒验证。
  README 的游戏截图和报告中的 MDR＋莫辛组合是不同样本。
- [整理性能报告](reports/organize-performance.md)与
  [预算分布报告](reports/grid-budget-profile.md)：规划、事务和搜索进展的测量。
- MVP 历史测量中，后续少量调整耗时数十毫秒，首次重排 360 件约 1.74 秒，
  用户未感到明显卡顿。记录见 [MVP 实施计划](plans/mvp.md)，
  该结果不代表后续聚合版本或其他库存的耗时承诺。
- [外部参考](../refs-local/README.md)：相关 mod、代码与资料来源。
